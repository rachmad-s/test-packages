export const helloWorld = () => {
    return 'Howdy!'
}

/**
 * adding two number
 * @param a number 1
 * @param b number 2
 * @returns number - result
 */
export const testFunction = (a: number, b: number): number => {
    return a + b
} 